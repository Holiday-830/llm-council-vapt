# LLM Council (VAPT) — cross-vendor parallel mode dependencies.
# Install:  pip install -r requirements.txt
#
# Versions are pinned to a floor known to expose the APIs the script uses
# (Messages API, chat.completions with max_completion_tokens, google-genai
# Client().models.generate_content). Newer versions are fine.

anthropic>=0.40.0
openai>=1.60.0
google-genai>=1.0.0
python-dotenv>=1.0.0
