# LLM Council (VAPT) — provider keys for cross-vendor parallel mode.
# Copy this file to ".env" and fill in real values. NEVER commit the real .env.
#
#   cp .env.template .env
#
# Get keys from:
#   Anthropic  -> https://console.anthropic.com/  (Settings > API Keys)
#   OpenAI     -> https://platform.openai.com/api-keys
#   Google     -> https://aistudio.google.com/app/apikey
#
# For an org subscription, request these from your org admin rather than
# generating personal keys.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
